import { styled } from 'baseui'

const DetailDes = styled('div', ()=> {
    return {
        fontSize: '20px',
        textAlign: 'center',
        color: '#272928',
        marginTop: '10px',
        marginBottom: '10px',
        fontWeight: '600'
    }
})

export default DetailDes
